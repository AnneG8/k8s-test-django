apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: edu-anna-veselova
  labels:
    env: dev
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - name: nginx-app-listener
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31257
