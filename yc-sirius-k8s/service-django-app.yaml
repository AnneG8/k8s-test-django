apiVersion: v1
kind: Service
metadata:
  name: django-app-service
  namespace:
  labels:
    env: dev
spec:
  type: NodePort
  selector:
    app: django
  ports:
    - name: app-listener
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort:
